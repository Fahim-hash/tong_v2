'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const [email,setEmail]=useState('');const router=useRouter();return <div className="wrap article"><div className="card"><div className="eyebrow">EDITORIAL</div><h1>লগইন</h1><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input" type="password" placeholder="Password"/><button className="button" onClick={()=>{if(email.trim()){localStorage.setItem('tk_user_session',email);router.push('/dashboard')}}}>প্রবেশ করুন</button></div></div>}
