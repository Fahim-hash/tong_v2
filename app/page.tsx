import Link from 'next/link';
import {articles} from './lib/data';
export default function Home(){const [lead,...rest]=articles;return <div className="wrap"><section className="hero"><div className="eyebrow">টং-এর খবর</div><h1>চায়ের কাপে শহরের গল্প।</h1><p>Spreading tea from a tong to the world — খবর, গল্প ও মানুষের কথা এক জায়গায়।</p></section><section className="grid"><article className="card"><Link className="story" href={`/articles/${lead.slug}`}><div className="meta">{lead.category} · {lead.publishedAt}</div><h2>{lead.title}</h2><p>{lead.excerpt}</p><span className="button">গল্পটি পড়ুন</span></Link></article><div>{rest.map(a=><article className="card" key={a.slug}><Link className="story" href={`/articles/${a.slug}`}><div className="meta">{a.category} · {a.publishedAt}</div><h3>{a.title}</h3><p>{a.excerpt}</p></Link></article>)}</div></section><section className="section"><h2>আজকের খবর</h2><div className="chips">{articles.map(a=><Link className="chip" key={a.slug} href={`/articles/${a.slug}`}>{a.title}</Link>)}</div></section></div>}
