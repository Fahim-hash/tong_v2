import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const {headline=''}=await req.json();if(!headline.trim())return NextResponse.json({caption:''},{status:400});if(!process.env.GEMINI_API_KEY)return NextResponse.json({caption:`${headline} — টং-এর খবর থেকে সর্বশেষ আপডেট। #টংএরখবর #বাংলাদেশ`});const {GoogleGenAI}=await import('@google/genai');const ai=new GoogleGenAI({apiKey:process.env.GEMINI_API_KEY});const response=await ai.models.generateContent({model:'gemini-2.5-flash',contents:`Write a concise Bengali social media news caption for this headline: ${headline}`});return NextResponse.json({caption:response.text||''})}catch(e){return NextResponse.json({caption:'কনটেন্ট তৈরি করা যায়নি। আবার চেষ্টা করুন।'},{status:500})}}
