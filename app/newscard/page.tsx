'use client';import {useState} from 'react';export default function NewsCard(){const [headline,setHeadline]=useState('');const [caption,setCaption]=useState('');async function generate(){const r=await fetch('/api/generate-caption',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({headline})});const j=await r.json();setCaption(j.caption||'')};return <div className="wrap article"><div className="eyebrow">NEWS CARD STUDIO</div><h1>Social Card</h1><input className="input" value={headline} onChange={e=>setHeadline(e.target.value)} placeholder="Headline"/><button className="button" onClick={generate}>AI Caption</button><textarea className="input" value={caption} onChange={e=>setCaption(e.target.value)} placeholder="Caption" rows={8}/><div className="card"><h2>{headline||'আপনার হেডলাইন'}</h2><p>{caption||'আপনার সোশ্যাল কপি এখানে দেখা যাবে।'}</p></div></div>}
