'use client';import {useState} from 'react';export default function Editor(){const [title,setTitle]=useState('');const [body,setBody]=useState('');return <div className="wrap article"><div className="eyebrow">EDITOR</div><h1>নতুন গল্প</h1><input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Headline"/><textarea className="input" value={body} onChange={e=>setBody(e.target.value)} placeholder="Story body" rows={12}/><button className="button" onClick={()=>alert('Draft saved locally for this v2 prototype.')}>Draft save</button></div>}
