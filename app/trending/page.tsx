import Link from 'next/link';import {articles} from '../lib/data';export default function Trending(){return <div className="wrap section"><div className="eyebrow">ট্রেন্ডিং</div><h1>এখন যে গল্পগুলো পড়ছেন</h1>{articles.map((a,i)=><article className="card" key={a.slug}><Link className="story" href={`/articles/${a.slug}`}><div className="meta">#{i+1} · {a.category}</div><h2>{a.title}</h2><p>{a.excerpt}</p></Link></article>)}</div>}
