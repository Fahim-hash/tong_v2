import Link from 'next/link';import {articles} from '../lib/data';
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const term=q.toLowerCase();const results=articles.filter(a=>`${a.title} ${a.excerpt} ${a.category}`.toLowerCase().includes(term));return <div className="wrap search"><h1>খুঁজুন</h1><form><input className="input" name="q" defaultValue={q} placeholder="খবর খুঁজুন..."/><button className="button">সার্চ</button></form><div className="section">{q&&<p className="meta">“{q}” — {results.length}টি ফলাফল</p>}{results.map(a=><article className="card" key={a.slug}><Link className="story" href={`/articles/${a.slug}`}><h2>{a.title}</h2><p>{a.excerpt}</p></Link></article>)}</div></div>}
